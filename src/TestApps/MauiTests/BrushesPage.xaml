<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MauiTests"
             x:Class="MauiTests.BrushesPage">

    <Grid >
        <CollectionView x:Name="collectionView" Grid.Row="1" Margin="20,5" ItemSizingStrategy="MeasureFirstItem">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <HorizontalStackLayout x:DataType="local:BrushEntry">
                        <Border WidthRequest="100" HeightRequest="40" Background="{Binding Brush}" Margin="0,2,5,2" StrokeThickness="0" />
                        <Label Text="{Binding Name}" VerticalOptions="Center" />
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>